const Imports = require("./src/modules.js");
const passportGoogle = require("./src/google.js");
const uses = require("./src/appuse.js");


app.listen(port, () => {
  console.log(`Server is running at port ${port}`);
});
